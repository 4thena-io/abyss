export interface Template {
  id: string;
  name: string;
  description: string;
  kind: string;
  language: string;
  repo_url: string;
  clone_url: string;
  created_at: string;
  updated_at: string;
}
